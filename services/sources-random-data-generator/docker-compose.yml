version: "2"
services:
  sources-random-data-generator:
    image: streampipes/sources-random-data-generator:${SP_PE_VERSION}
    depends_on:
      - "consul"
#    ports:
#      - "8098:8090"
    environment:
      - SP_ICON_HOST=${SP_ICON_HOST}
      - SP_KAFKA_HOST=${SP_KAFKA_HOST}
    networks:
      spnet:
